import type { ModuleOptions } from '@nuxtjs/tailwindcss'

export default {
  theme: {
    extend: {
      colors: {
        background: {
          dark: '#161616',
        },
      },
    },
  },
} as ModuleOptions['config']
